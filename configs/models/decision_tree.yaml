# Decision Tree baseline. The random seed comes from the experiment config.
# Hyperparameters for the reproduction study are fixed in M2 after reviewing the
# reference paper's reported settings.
name: decision_tree
params:
  max_depth: 12
  min_samples_leaf: 2
