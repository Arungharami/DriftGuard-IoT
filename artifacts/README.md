# Artifacts

Serialized models and their matching fitted preprocessing pipelines. Contents are
git-ignored; artifacts are published only through the approved Hugging Face release path
(M7) after evaluation, with a model card and the run manifest that produced them.
