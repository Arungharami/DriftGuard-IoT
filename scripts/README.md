# Scripts

| Script | Purpose |
| --- | --- |
| `check_repo_hygiene.py` | Blocks datasets, serialized artifacts, credentials and files > 2 MiB (CI + optional pre-commit) |

Planned: `publish_hf.py` (M7): uploads an evaluated artifact and its model card only after
an explicit approval flag, with a research-kind manifest check.
